1. Visual Studio Solution should choose while files to upload?
    '.gitignore' 中添加忽略的文件和目录
2. 建立仓库有1G的限制，不能太大了，可以多件几个仓库
3. 通过VS创建的仓库会自带VS的.gitignore 和 .gitattribute
4. git的简单总结
    1. 远程有个仓库，本地有个仓库，分支是协同工作
    2. 新建的仓库必须为空文件夹
